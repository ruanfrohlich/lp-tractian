.grid
  display flex  
  flex-direction column
  row-gap 60px

  +above($md)
    display grid
    grid-template-columns 1fr
    grid-gap 60px

  +above($lg)
    grid-template-columns repeat(2, minmax(0, 1fr))
    grid-gap 20px